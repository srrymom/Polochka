<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.Polochka" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here -->
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/montserrat</item>
        <item name="android:fontWeight">400</item>

        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Polochka" parent="Base.Theme.Polochka" />

    <!-- Base button style -->
    <style name="BaseButtonStyle">
        <item name="android:layout_width">50pt</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@drawable/buttonstyle</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAllCaps">false</item>


    </style>

    <!-- Specific button styles -->
    <style name="MessageButtonStyle" parent="BaseButtonStyle">
        <item name="android:backgroundTint">@color/primary</item>
    </style>


    <style name="FavoriteButtonStyle" parent="BaseButtonStyle">
        <item name="android:backgroundTint">@color/green</item>
    </style>
</resources>
